from setuptools import setup, find_packages

setup(
    name='py_sipoc_mr_solver',
    version='0.0',
    packages=find_packages(),
    install_requires=[
        # Add any dependencies here
    ],
)
